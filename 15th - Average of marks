#include<iostream>
using namespace std ;

int main(){
    int n;
    cin>>n; 
    int *marks=new int[n];
    int sum=0; 
    for (int i=0;i<n;i++){
        cin>>marks[i];
        sum+=marks[i];
            }
    float avg=float(sum)/n;
    cout<<avg<<endl ;
    return 0;
}
