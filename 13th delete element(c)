#include <stdio.h>
int main() {
    int i,n,pos;
        printf("enter array size :\n");
    scanf("%d",&n);
    int arr[n];
        printf("enter array : \n");
    for (int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
        printf("enter position: \n");
    scanf("%d",&pos);
    if(pos<1 || pos>n){
        printf("invalid");
    }
    else{
        for(i=pos-1;i<n-1;i++){
            arr[i]=arr[i+1];
            
        }
        n--;
        printf("your new array is :\n");
        for(int i=0;i<n;i++){
            printf("%d ",arr[i]);
        
        }
    }
    return 0;
}
